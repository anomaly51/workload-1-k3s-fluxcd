apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: shisha-backend
  namespace: flux-system
spec:
  image: harbor.api-api-api.com/shisha/backend
  interval: 1m
  secretRef:
    name: harbor-image-pull-secret
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: shisha-backend
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: shisha-backend
  filterTags:
    pattern: '^main-(?P<build>[0-9]+)-.*$'
    extract: '$build'
  policy:
    numerical:
      order: asc
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageUpdateAutomation
metadata:
  name: shisha-backend
  namespace: flux-system
spec:
  interval: 1m
  sourceRef:
    kind: GitRepository
    name: flux-system
  git:
    checkout:
      ref:
        branch: main
    commit:
      author:
        name: fluxbot
        email: flux@anomaly51
      messageTemplate: "chore: update shisha-backend image"
    push:
      branch: main
  update:
    path: ./apps/shisha-guid
    strategy: Setters
